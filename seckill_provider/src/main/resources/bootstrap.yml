spring:
  application:
    #此处的应用对应于Nacos Config中的Data ID
    name: seckill-provider-config
    #查找指定名称为seckill-provider-config.yaml的配置文件
  cloud:
    nacos:
      config:
        file-extension: yaml
        server-addr: 150.158.156.214:8848